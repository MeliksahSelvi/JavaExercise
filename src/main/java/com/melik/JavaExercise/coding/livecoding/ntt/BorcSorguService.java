package com.melik.JavaExercise.coding.livecoding.ntt;

public class BorcSorguService {

    /*
    public double calculateBorc(List<Object> urunler) {
        double borc = 0;
        for (Object urun : urunler) {
            if (urun instanceof CreditCard) {
                CreditCard creditCard = (CreditCard) urun;
                borc += (creditCard.getAnaPara() + creditCard.getFaiz()+ creditCard.getGecikmeFaizi() );
            } else if (urun instanceof MortgageCredit) {
                MortgageCredit mortgage = (MortgageCredit) urun;
                borc += (mortgage.getAnaPara() + mortgage.getKomisyon() );
            } else if (urun instanceof VergiBorcu) {
                VergiBorcu vergiborc = (VergiBorcu) urun;
                borc += (vergiborc.getTutar() );
            } else {
                throw new RuntimeException("urun not supported");
            }
        } return borc;
    }*/
}